# 기획 스킬 세팅 가이드 — Claude Code 가 읽고 사용자를 이끄는 대본

이 문서는 사람이 읽는 설명서이면서, 이 폴더(릴스 레퍼런스 수집기)에서 실행된 Claude Code 가 그대로 따라가는 실행 지시서다.
사용자가 "기획 스킬 세팅해줘", "3단계 진행해줘", "구축 가이드대로 진행해줘"라고 하면 Claude Code 는 이 문서를 열고 0단계부터 한 단계씩 진행한다.

## Claude Code 가 지킬 원칙

1. 터미널은 Claude 가, 브라우저는 사용자가. 사용자에게 명령어를 치게 하거나 파일을 열어 고치게 하지 않는다. 설정 값은 Claude 가 `data/config.json` 을 직접 고쳐도 된다(서버가 켜져 있어도 다음 요청부터 반영된다).
2. 한 메시지에 행동 하나. 단계 끝은 "다 되셨으면 '됐어요'라고 답해주세요" 한 문장으로 끝낸다.
3. 확인 전에는 다음 단계로 가지 않는다. 단계마다 아래 "검증"을 기계로 확인한다.
4. 왜 하는지 한 줄. 설명은 한 줄이면 충분하다.
5. 사용자를 부르는 호칭은 쓰지 않는다. 존댓말. 전문 용어는 괄호로 풀어 쓴다.
6. `.env` 의 값은 채팅에 다시 출력하지 않는다.

---

## 0단계 · 자리 확인 (30초)

- 검증: 현재 폴더에 `server.js`, `data/refs.json`, `.claude/skills/릴스레퍼런스기획/SKILL.md` 가 있다. `bash .claude/skills/릴스레퍼런스기획/lib/fetch.sh mode` 가 "reels-refs 모드"를 낸다.
- 없으면: "이 폴더는 수집기 폴더가 아니에요. 시작.command 가 있는 폴더에서 열어주세요." 로 멈춘다.
- `fetch.sh ctx` 를 돌려 레퍼런스 카드 수와 분석됨 수를 확인한다. 카드가 0이면 "수집기에서 수집을 먼저 한 번 돌려주세요"로 멈춘다.

## 1단계 · 내 계정과 대본 (보통 0분, 옛 수집기면 5분)

기획 스킬은 내 릴스 대본으로 "내 말투(DNA)"를 배운다. 최신 수집기(1단계 v5.5 이상)는 내 상위 10개 릴스 대본을 기본으로 받아 두므로 대개 그대로 통과한다.

1. `fetch.sh ctx` 의 핸들이 "(설정 안 됨)"이면: "수집기 화면의 '내 계정 & 경쟁사' 탭 오른쪽 위 '내 계정' 칸에 인스타 아이디를 넣고 저장해 주세요. 다 되셨으면 '됐어요'." → 확인 후 진행.
2. `fetch.sh ctx` 의 "대본이 있는 내 릴스"가 1 이상이면 이 단계는 끝. 0이면:
   - `data/config.json` 의 `myTranscripts` 를 `true` 로 바꾼다 (Claude 가 직접 수정. `myTranscriptTop` 은 10 유지).
   - 사용자에게: "수집기 설정 탭에서 '수집 시작'을 눌러주세요. 내 상위 10개 릴스 대본을 받아요(10크레딧, 이번 한 번). 2~3분 걸려요. 끝나면 '됐어요'." 서버가 꺼져 있으면 Claude 가 `node collect.js` 를 직접 돌려도 된다.
- 검증: `fetch.sh ctx` 의 "대본이 있는 내 릴스"가 1 이상.

## 2단계 · 계정 문서 만들기 (10분, 크레딧 0)

- `/계정세팅` 스킬을 그대로 따른다 (대표대본 → 재기 → 문서 5개 → 사용자 검토 3회 → 보고). 검토 질문 셋은 스킬 4절의 예시·답하는 법을 붙여서 묻는다 — 초보자가 "없어요"만 쳐도 넘어갈 수 있게.
- 검증: `계정/<핸들>/` 에 `프로필.md · DNA.md · 형식.md · 훅.md · 안티패턴.md · 공구광고.md · 대표대본/_index.md` 와 대표대본 파일 5건 이상.

## 3단계 · 첫 기획 (2분)

- 사용자에게: "이제 카드 번호로 부르면 돼요. 예를 들어 'R-001로 기획해줘'. 지금 한 번 해볼까요? 참고할 카드 번호를 말해주세요."
- 스킬이 시작 질문 셋을 내고, 답을 받아 후킹 3안을 낸다. 사용자가 고르면 대본·촬영메모를 쓴다. "저장"이라고 하면 `기획/` 에 저장.
- 검증: `기획/YYYY-MM-DD_*.md` 파일이 하나 생겼다.

## 그 뒤로

- 새 레퍼런스가 쌓이면 수집기에서 수집·분석만 돌리면 된다. 스킬은 항상 최신 `data/refs.json` 을 읽는다.
- 내 릴스가 늘어 상위 10개가 바뀌면 다음 수집 때 새로 상위에 든 것만 대본을 받는다. "대표대본 갱신"이라고 하면 문서에 반영한다.
- 말투가 바뀌었으면 "프로필 다시 만들어줘".
